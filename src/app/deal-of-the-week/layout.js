export const metadata = {
  title: 'Deal of the Week | Peptides Costa Rica',
  description: 'This week\'s peptide offer from Peptides Costa Rica: the discount, the eligible products and the terms. Updated every week.',
};

export default function DealOfTheWeekLayout({ children }) {
  return children;
}
